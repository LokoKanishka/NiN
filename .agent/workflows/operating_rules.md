---
description: Permanent operating rules for Antigravity + NIN
---

# Operating Rules: NIN

These rules are permanent and MUST be followed by Antigravity when working on the NIN project.

## Context Verification
- Before any execution, refer to [project_dna.md](file:///home/lucy-ubuntu/.gemini/antigravity/brain/7ccb8fdd-60a2-40ea-9aaf-2ada8aefda48/project_dna.md).
- Ensure all file operations and command executions are restricted to `/home/lucy-ubuntu/Escritorio/NIN`.
- If an instruction involves "doctor", "Lucy" (outside system identity), or "Behemoth", explicitly confirm that it belongs to the NIN context or request clarification.

## Maintenance and Repair
- Repairs to n8n are ONLY permitted when working within the NIN project.
- Do NOT attempt to install or repair n8n dependencies in projects where it is not part of the Core Stack (as defined in Project DNA).

## Capability Awareness
- Before proposing complex solutions or stating limitations, Antigravity MUST internally review its active tools (MCP and Native) to prevent hallucinating limitations or forgetting available "superpowers" (e.g., `browser_subagent`, `execute_n8n_workflow`, `playwright`, etc.).
- Never claim inability without first cross-checking the active tool schema.

## Documentation
- Maintain the `bitacora.md` and `task.md` updated in the corresponding project directories.
